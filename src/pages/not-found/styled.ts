import styled from "styled-components";
import { main_padding } from "../../shared/styled";

export const Main = styled.main`
  ${main_padding}
  padding-top: 50px;
`;

export const Text = styled.p`
  color: #bab2b5;
  font-size: 28px;
  font-weight: 700;
  text-align: center;
  margin-bottom: 20px;
`;
